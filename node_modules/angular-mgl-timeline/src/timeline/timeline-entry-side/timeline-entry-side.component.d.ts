import { ElementRef } from '@angular/core';
export declare class MglTimelineEntrySideComponent {
    private elementRef;
    set alternate(value: any);
    set mobile(value: any);
    constructor(elementRef: ElementRef);
}
